fastapi==0.135.3
uvicorn[standard]==0.38.0
python-multipart==0.0.26
pillow==9.5.0
numpy==1.26.4
opencv-python==4.10.0.84
scikit-image==0.21.0
cryptography==46.0.3
rembg==2.0.67
onnxruntime==1.20.1
# DeOldify vendored fastai runtime still imports pkg_resources.
# setuptools>=81 removes pkg_resources, so keep setuptools below that cut.
setuptools<81
torch==2.0.1
torchvision==0.15.2
realesrgan==0.3.0
gfpgan==1.3.8
basicsr==1.4.2
facexlib==0.3.0
opencv-python-headless==4.10.0.84
gdown==5.2.0
einops==0.8.0
hydra-core==1.3.2
omegaconf==2.3.0
ffmpeg-python==0.2.0
yt-dlp==2025.1.26
ipython==8.37.0
# NOTE: pip's strict resolver rejects this package with torch==2.0.1 because
# upstream metadata excludes torch 2.0.1. For this project's known-good stack,
# install the rest of requirements first, then install this package with:
#   pip install --no-deps simple-lama-inpainting==0.1.2
fastai==2.7.19
